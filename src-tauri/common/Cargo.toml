[package]
name = "common"
version = "1.0.0"
edition = "2021"
rust-version = "1.80"
license-file = "../../LICENSE.md"

[dependencies]

[target.'cfg(unix)'.dependencies]
nix = { version = "0.29", features = ["net"] }
